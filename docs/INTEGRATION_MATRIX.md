# INTEGRATION MATRIX

Status values used below (per MESH spec §58, the UI/docs must distinguish these, not blur them):

- **LIVE** — a real running connection exists today.
- **SNAPSHOT** — real data, copied via a provenance-hashed sync step (like risk-swarm's
  `sync-snapshots.mjs`), not a live call.
- **PLANNED** — designed, not built.
- **BLOCKED** — cannot be built honestly right now (missing credentials/runtime).
- **NOT_CONNECTED** — no integration exists anywhere in the ecosystem today.

| System | Today (found in Phase 0) | MESH plan | Status |
|---|---|---|---|
| risk-swarm | Already snapshot-syncs FOMO, freight-risk-atlas, ai-governance-control-room; has its own council/disagreement/evidence-tier model, but pushes no persisted council/decision export anywhere on disk (runs entirely in-browser against a user's own BYOK key) | **Built** (`adapters/risk-swarm/`): type-level mirror of `CouncilResult` + pure mapping functions, no fetch step (nothing to fetch). `councilToDisagreement()` fails closed rather than counting a degraded fallback position as independent. Fixtures are real: captured by actually running risk-swarm's own `runCouncil()` with the same fetch-mock technique its own test suite uses. Still PLANNED: a real live/snapshot channel so the caller can supply a genuine BYOK-run result instead of a test-technique one. | **MOCKED** (adapter built and tested; every mapped object honestly carries whatever `provenanceSource` the caller states) |
| risk-replay | Real FastAPI backend, 13 engines, SQLAlchemy persistence | **Built and live-verified** (`adapters/risk-replay/`): ran the real backend locally (`uv run uvicorn app.api.main:app`), called `/decisions/DEC-001/counterfactual` for real, captured the response as a fixture. Removing evidence `E3` flips DEC-001 BLOCK→ALLOW; risk-replay's own `causal_engine.py` returns `causal_status: DECISION_CRITICAL` with the exact phrase spec §18 anticipates ("decision-critical under the replay model"). Discovered risk-replay's real `MutationType` enum only partially overlaps MESH's (5 of 9/10 values agree) — mapped only the five that mean the same thing, left the rest explicitly unmapped rather than guessed. | **LIVE** (adapter built; requires the backend running locally, `RISK_REPLAY_API_BASE_URL` env var) |
| policy-audit | Client-side framework/control/evidence engine; export path not yet verified | MESH sends case+decision, receives governance findings, once an export/import path is confirmed to exist | PLANNED |
| fraud-watch | Real `moEngine.mos` in `data/world-state.json`: structured MO records (id, classification, status, signature, entities, signals, evidence). Earlier PLANNED entry assumed no such object type existed anywhere in fraud-watch's code — that assumption was wrong, corrected in Phase 5. | **Built** (`adapters/fraud-watch/`): reads the real on-disk state file (fraud-watch is static, no server), maps 4/8 `BehaviorKind` values (3 exact-name matches + `POTENTIAL_NEW_MO`→`EDGE_CASE`, semantic not literal, because fraud-watch's own code says that label is not a novelty claim). `COMPOSITE_MO`/`CANDIDATE_NEW_MO`/`BENIGN_VARIANT`/`FALSE_POSITIVE` have no fraud-watch source, left unmapped. Every mapped `Behavior` is unconditionally `SIMULATED` (§16); fraud-watch's own investigation status is preserved in `provenance.note`, never conflated with `Behavior.status`. `CandidateMo` (§17) is a separate, later MESH-side pipeline, not derived directly from one MO record. | **SNAPSHOT** (adapter built and tested against a real captured world-state.json; every mapped object carries `provenance.source: SIMULATED`) |
| risk-os | Own typed risk-register domain model + validators | MESH proposes candidate risks in the exact shape risk-swarm's `riskos.ts` already uses — reuse that contract rather than invent a second one | PLANNED |
| risk-ring | Trained classifier + graph, own test suite (3 files) | Association/correlation/suspicion/validated-relationship distinction (§23) read from risk-ring's own graph output | PLANNED |
| FOMO | Already feeds risk-swarm today | **Built** (`adapters/fomo/`): reads FOMO's real synced signals straight from risk-swarm's own hash-verified `public/snapshots/fomo/signals.json` rather than re-syncing from FOMO's repo directly (Phase 0 finding: don't duplicate a sync step an adapter already does). Every mapped `Signal` is unconditionally `status: 'RAW'` (§24, Rule 4) — promotion to Evidence stays the Evidence Fabric's job. | **SNAPSHOT** (adapter built and tested against a real trimmed fixture of risk-swarm's synced fomo/signals.json; shared hash-verification helper fails closed on any provenance.json mismatch) |
| freight-fraud-taxonomy / freight-risk-atlas | Already the taxonomy source risk-swarm syncs (hashed) | **Built** (`adapters/freight-risk-atlas/`): reads risk-swarm's own hash-verified `public/snapshots/freight-risk-atlas/taxonomy.json`; `taxonomyVersionRecord()` feeds `CandidateMo.taxonomy_version`/`taxonomy_hash` (§17) directly from risk-swarm's already-verified sha256, never re-hashed independently. | **SNAPSHOT** (adapter built and tested against a real trimmed fixture: real `meta` + first 2 of 12 real patterns) |
| ai-governance-control-room | Already snapshot-synced into risk-swarm | Portfolio-level control plane per §20 — MESH exposes records *to* it rather than pulling from it | PLANNED |
| forecast-ledger | Python, sealing/grading logic in `METHODOLOGY.md`/`SPEC.md`, not yet code-verified | MESH-issued forecasts get sealed there; not started | PLANNED |
| Laya (3 checkpoints) | Never integrated anywhere in the ecosystem | **Built** (`adapters/model-registry/`): 3 real checkpoints registered (laya-english, laya-multilingual, laya-typed, per docs/MODEL_ARENA.md), every entry `NOT_CONNECTED`; `callModel()` has no success path, only the specific missing dependency named | **BLOCKED** (registry + honest-failure adapter built and tested — no HF runtime/credentials in this environment) |
| Jev / open-jev family | Never integrated anywhere in the ecosystem | **Built** (`adapters/model-registry/`): both registered, `NOT_CONNECTED`; empirical comparison against Laya remains PLANNED, since neither model is callable yet | **BLOCKED** (registry + honest-failure adapter built and tested — no Jev API access) |
| hakai-protocol-v2, hakai_world | Explicitly OPTIONAL/FUTURE in the directive | Deferred | NOT_CONNECTED, out of scope for now |

## Honesty rule enforced in code, not just docs

The `Provenance` contract (Phase 1) requires every MESH object to carry a `provenance.source` that is
one of `LIVE | SNAPSHOT | SIMULATED | MOCKED | CACHED | UNAVAILABLE` (see `contracts/schemas.ts`). The
schema cannot verify that a claimed label is *true* (no type system can), but it does close one real
gap: nobody can produce a valid MESH object with a blank, missing, or invented seventh label — every
object is forced to pick one of these six honest words, and any adapter for a BLOCKED integration
(Laya, Jev today) has no way to emit anything at all until it exists, so there is nothing to mislabel.
