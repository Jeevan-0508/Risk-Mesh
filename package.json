{
  "name": "risk-mesh",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "author": "Jeevan Siddhabhaktula",
  "type": "module",
  "description": "RISK//MESH - the connective contract, evidence and provenance layer between independent risk systems",
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc -b --noEmit",
    "observatory:capture": "bun run observatory/scripts/capture-golden-events.ts",
    "observatory:capture:system1": "bun run observatory/scripts/capture-system1-events.ts",
    "observatory:capture:registry": "bun run observatory/scripts/capture-repo-registry.ts",
    "laya:smoke": "bun run adapters/model-registry/live-smoke.ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "typescript": "^5.6.3"
  }
}
